import { UniqueBoardNameValidator } from './unique-board-name-validator';

describe('UniqueBoardNameValidator', () => {
  it('should create an instance', () => {
    expect(new UniqueBoardNameValidator()).toBeTruthy();
  });
});

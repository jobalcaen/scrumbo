build:
  docker:
    web: backend/Dockerfile.prod

run:
  web: daphne -b 0.0.0.0 -p $PORT my_app.wsgi:application
  worker:
    command:
      - python manage.py runworker -v2
    image: web

release:
  image: web
  command:
    - python manage.py collectstatic --noinput



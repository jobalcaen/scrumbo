build:
  docker:
    web: backend/Dockerfile.prod

run:
  web: daphne my_app.wsgi:application
  worker:
    command: python myworker.py
    image: web

release:
  image: web
  command:
    - python manage.py collectstatic --noinput

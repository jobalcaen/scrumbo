build:
  docker:
    web: backend/Dockerfile.prod

run:
  web: daphne -b 0.0.0.0 -p 8000 my_app.asgi:application
  worker:
    command:
      - python manage.py runworker
    image: web

release:
  image: web
  command:
    - python manage.py collectstatic --noinput


